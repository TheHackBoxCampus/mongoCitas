export let errs = {
    "properties": {
        status: 400, message: "Ops!, lo sentimos, parece que algunas propiedades son incorrectas!"
    },
    "required": {
        status: 400, message: "Vaya!, hay campos que deben ser llenados!"
    }
}
